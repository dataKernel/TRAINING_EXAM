#include <stdio.h>

#define MIN		0
#define MAX		5

int		ft_ultimate_range(int **range, int min, int max)
{
	return(0);
}

int		main(void)
{
	int		*tabInt;

	tabInt = ft_ultimate_range(tabInt, MIN, MAX);
	return(0);
}